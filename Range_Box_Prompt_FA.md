# دستورالعمل جامع «باکس رنج» (Range Box)

> وضعیت: **نسخه‌ی جامع برای تأیید نهایی**. هنوز در کد پیاده نشده است (کد فعلی V15.2 نسخه‌ی قبلی قواعد را دارد).
> پیش‌نیاز: `Real_Breakout_Prompt_FA.md` (۳ شرط شکست و ۳ شرط پولبک) و `Market_Structure_Prompt_FA.md` (چرخه‌ی روند، CTS و BOS).
> موارد ❓ در بخش ۱۰ منتظر تأیید هستند.

---

## ۱) نقش و هدف
تو یک تحلیلگر پرایس اکشن هستی. وظیفه‌ات این است که در هر تایم‌فریم، **محدوده‌های رنج** را درست تشخیص دهی، رسم کنی و ببندی. هدف این است که:
- نویز (حرکت بدون شکست یا پولبک واقعی) هیچ‌وقت CTS یا BOS نسازد.
- ساختار بازار فقط از **شکست واقعی** و **پولبک واقعی** ساخته شود.

**قانون طلایی رنج:** تا وقتی شکست واقعی یا پولبک واقعی رخ نداده، قیمت داخل رنج است، هر چقدر بالا یا پایین برود. **تنها معیار پایان رنج، شرط‌های شکست واقعی و پولبک واقعی است.**

---

## ۲) یادآوری تعاریف پایه
| مفهوم | تعریف |
|---|---|
| ماروبوزو | بدنه دست‌کم ۷۰٪ (گاهی ۶۰٪) کل کندل |
| همیشه | طول **کل** کندل (High−Low) مقایسه می‌شود، نه بدنه |
| شکست واقعی | یکی از ۳ شرط شکست (فایل پیش‌نیاز) |
| پولبک واقعی | یکی از ۳ شرط پولبک، بدون هیچ شرط اضافه (حجم و غیره) |
| مرحله‌ی الف | موج اصلی؛ هنوز پولبک واقعی رخ نداده |
| مرحله‌ی ب | شرایط پولبک رخ داده؛ منتظر شکست واقعی CTS |
| CTS | سقف (روند صعودی) یا کف (روند نزولی) موج، که فقط با شکست واقعی تأیید می‌شود |

---

## ۳) دو نوع باکس رنج

| | **باکس نوع ۱: رنج بلاتکلیفی** | **باکس نوع ۲: باکس پولبک** |
|---|---|---|
| **چه زمانی** | مرحله‌ی الف: حرکت خلاف روند **بدون** پولبک واقعی | مرحله‌ی ب: بعد از رخ دادن شرایط پولبک واقعی، تا شکست واقعی CTS |
| **شروع** | کندل ۱ + کندل ۲ (بخش ۴) | از **خط CTS** (سقف یا کف قفل‌شده‌ی موج) |
| **پایان** | فقط **شرط ۱** شکست یا پولبک (بخش ۶) | **یکی از ۳ شرط** شکست واقعی CTS (بخش ۷) |
| **نتیجه‌ی پایان در جهت روند** | CTS و BOS **شناسایی نمی‌شود** | **CTS و BOS با هم** شناسایی می‌شوند |

---

## ۴) باکس نوع ۱: تشکیل
1. **کندل ۱ (شروع):** اولین کندل خلاف روند. در روند صعودی نزولی، در روند نزولی صعودی. شکل مجاز: پین‌بار، دوجی، نرمال (بدنه ۵۰ تا ۷۰٪) یا ماروبوزو (بیش از ۷۰٪). کندل ۱ همیشه شروع باکس است.
2. **کندل ۲ (تأیید):** یکی از این‌ها:
   - اینساید بار کامل
   - حداکثر ۳۰٪ از کل طول کندل ۲ بیرون از کندل ۱
   - پین‌بار یا دوجی
   - نرمال، فقط اگر کندل ۱ پین‌بار یا دوجی باشد و قاعده‌ی ۳۰٪ برقرار باشد
3. اگر کندل ۱ و ۲ خودشان **پولبک واقعی** بسازند، باکس تشکیل **نمی‌شود**. آن حرکت پولبک است و مرحله‌ی ب شروع می‌شود.
4. **مرز اولیه:** High و Low کندل ۱. این مرز **ایستا نیست** و طبق بخش ۵ گسترش پیدا می‌کند.
5. بعد از هر شکست واقعی، اولین کندل خلاف روند کاندید کندل ۱ است. باکس فقط وقتی باز می‌شود که کندل ۲ هم شرایط را داشته باشد.

---

## ۵) گسترش باکس (هر دو نوع)
1. هر کندلی که از باکس بیرون برود، با **سایه** یا حتی **بخشی از بدنه**، ولی **شکست واقعی یا پولبک واقعی نسازد**، باکس را **به بالا، پایین یا راست** گسترش می‌دهد تا سایه‌اش داخل باکس قرار بگیرد. مهم نیست بالا برود یا پایین.
2. **کندل‌های فیک بریک‌اوت:** اگر قیمت سطحی را کاذب بشکند، باکس به دنبال همان کندل‌ها گسترش پیدا می‌کند و آن‌ها جزو رنج می‌شوند.
3. **قاعده‌ی دو کندل:** شکست و پولبک واقعی الگوی دوکندلی هستند.
   - وقتی کندلی **بیرون از مرز باکس بسته می‌شود** (کندل اول احتمالی الگو)، مرز باکس **فقط برای کندل بعدی** ثابت می‌ماند تا الگو نسبت به مرز واقعی سنجیده شود.
   - اگر این **۲ کندل** الگوی پایان باکس را بسازند ← باکس بسته می‌شود.
   - اگر نسازند ← **هر دو کندل جزو باکس** می‌شوند و باکس گسترش پیدا می‌کند.

---

## ۶) باکس نوع ۱: پایان (فقط شرط ۱)
برای خروج از باکس نوع ۱ **فقط شرط ۱** به کار می‌رود. شرط ۲ و ۳ برای این خروج استفاده **نمی‌شوند**، ولی در بقیه‌ی قواعد (شکست CTS، پولبک بیرون از رنج) معتبرند.

| رویداد (مثال: روند صعودی) | شرط دقیق | نتیجه |
|---|---|---|
| **شکست واقعی در جهت روند** | دو ماروبوزوی **پیوسته و هم‌جهت** (صعودی)، **هر دو بالای سقف باکس بسته شوند**، کندل دوم بالاتر از **کل** کندل اول (High آن) بسته شود | باکس بسته می‌شود. **CTS و BOS شناسایی نمی‌شود.** مرحله‌ی الف ادامه دارد. |
| **پولبک واقعی که باکس را خلاف روند بشکند** | دو ماروبوزوی **پیوسته و هم‌جهت** (نزولی)، **هر دو زیر کف باکس بسته شوند**، کندل دوم پایین‌تر از Close کندل اول، طول کل کندل دوم دست‌کم ۷۰٪ کندل اول | باکس بسته می‌شود. مرحله‌ی ب شروع می‌شود؛ پولبک **در انتظار تأیید** است (بخش ۸). |
| **شکست واقعی خلاف روند** | دو ماروبوزوی پیوسته و هم‌جهت (نزولی)، هر دو زیر کف، کندل دوم پایین‌تر از **کل** کندل اول (Low آن) | باکس بسته می‌شود. |
| **هر حرکت دیگر** (از جمله الگوی پولبک **داخل** باکس که باکس را نشکند) | — | نویز رنج؛ باکس ادامه دارد و طبق بخش ۵ گسترش پیدا می‌کند. |

روند نزولی دقیقاً قرینه است: بالا ↔ پایین، سقف ↔ کف، صعودی ↔ نزولی.

---

## ۷) باکس نوع ۲: باکس پولبک
1. **شروع:** وقتی شرایط پولبک واقعی رخ دهد (مرحله‌ی ب)، باکس از **خط CTS** شروع می‌شود. خط CTS همان سقف قفل‌شده‌ی موج در روند صعودی، یا کف قفل‌شده در روند نزولی است.
2. **مرزها:**
   - یک طرف باکس خط CTS است.
   - طرف دیگر، دورترین نقطه‌ی موج پولبک است و با حرکت قیمت گسترش پیدا می‌کند.
3. **فیک بریک‌اوت CTS:** اگر قیمت از خط CTS عبور کند ولی هیچ‌یک از ۳ شرط شکست واقعی رخ ندهد:
   - باکس به دنبال همان کندل‌ها گسترش پیدا می‌کند.
   - **پولبک و CTS و BOS هنوز شناسایی نمی‌شوند.**
4. **پایان با شکست واقعی (یکی از ۳ شرط) ✅:**
   - شکست واقعی نسبت به **مرز گسترش‌یافته‌ی باکس** سنجیده می‌شود (دورترین نقطه‌ی کندل‌های فیک)، نه خط CTS اولیه.
   - بعد از این شکست، **CTS روی خط اصلی خودش** رسم می‌شود (سقف یا کف قفل‌شده‌ی موج، نه نقطه‌ی فیک‌ها).
   - **CTS** و **BOS** هم‌زمان شناسایی می‌شوند. BOS = دورترین نقطه‌ی موج پولبک.
   - پولبک در همین لحظه **تأیید** می‌شود (هشدار «Real Pullback» همین‌جا).
   - باکس بسته می‌شود و موج اصلی جدید شروع می‌شود.
5. **پایان با تغییر روند:** اگر آخرین BOS (سطح برگشت روند) با شکست واقعی شکسته شود، روند عوض می‌شود و باکس بسته می‌شود.
6. تا وقتی باکس نوع ۲ فعال است، **باکس نوع ۱ جدیدی** داخل آن تشکیل نمی‌شود. رنج‌های داخل موج پولبک روی ساختار اثری ندارند.

---

## ۸) زمان شناسایی پولبک
- پولبک واقعی **تا شکست واقعی CTS شناسایی نمی‌شود** و به تعویق می‌افتد، حتی اگر همه‌ی شرایطش رخ داده باشد.
- وقتی باکس نوع ۱ با الگوی پولبک بسته می‌شود، برچسبش **خنثی** است («Range Closed»)، نه «Real Pullback».
- برچسب و هشدار پولبک فقط در لحظه‌ی شکست واقعی CTS نمایش داده می‌شوند.

---

## ۹) نمونه‌ی تحلیل (چارت روند نزولی شما)
1. **باکس ۱:**
   - قیمت کف قبلی را کاذب شکست. باکس به دنبال کندل‌های فیک به پایین گسترش یافت.
   - رنج بدون پولبک واقعی رو به پایین شکسته شد. **کف باکس CTS نیست.**
2. **باکس ۲:**
   - از اولین کندل صعودی بعد از ریزش شروع شد.
   - رالی داخل باکس تا وسط باکس ۱ بالا رفت، ولی هیچ‌یک از ۳ شرط پولبک رخ نداد، پس رنج است. باکس تا بالاترین سایه گسترش یافت.
   - با شکست رو به پایین بسته شد. **CTS نیست.**
3. **باکس ۳:** همان الگو.
4. **کف L:**
   - جهش بعدی شرایط پولبک داشت ← **باکس نوع ۲** از خط L شروع می‌شود.
   - قیمت به L برگشت و زیرش رفت، ولی شکست واقعی نبود ← باکس به دنبال کندل‌های فیک گسترش پیدا می‌کند. **پولبک، CTS و BOS هنوز شناسایی نمی‌شوند.**

---

## ۱۰) الگوریتم گام‌به‌گام برای هر کندل
1. آیا باکس نوع ۲ فعال است؟
   - **شکست واقعی CTS (یکی از ۳ شرط):** CTS، BOS و تأیید پولبک ثبت می‌شوند و باکس بسته می‌شود.
   - **شکست واقعی BOS:** روند عوض می‌شود و باکس بسته می‌شود.
   - **در غیر این صورت:** باکس طبق بخش ۵ گسترش پیدا می‌کند.
2. آیا باکس نوع ۱ فعال است؟
   - **شرط ۱ شکست در جهت روند:** باکس بسته می‌شود؛ بدون CTS و BOS.
   - **شرط ۱ پولبک خلاف روند که باکس را بشکند:** باکس بسته می‌شود («Range Closed») و مرحله‌ی ب شروع می‌شود (باکس نوع ۲).
   - **شرط ۱ شکست خلاف روند:** باکس بسته می‌شود.
   - **در غیر این صورت:** باکس طبق بخش ۵ گسترش پیدا می‌کند.
3. هیچ باکسی فعال نیست؟
   - اگر پولبک واقعی (یکی از ۳ شرط) رخ دهد ← مرحله‌ی ب و باکس نوع ۲.
   - وگرنه، اگر کندل ۱ و ۲ شرایط را داشته باشند ← باکس نوع ۱.

---

## ۱۱) موارد نیازمند تأیید ❓
1. ✅ **سطح شکست در باکس نوع ۲:**
   - شکست واقعی نسبت به **مرز گسترش‌یافته‌ی باکس** سنجیده می‌شود.
   - CTS روی **خط اصلی خودش** رسم می‌شود.
2. **طرف دیگر باکس نوع ۲:** دورترین نقطه‌ی موج پولبک است (در روند نزولی، بالاترین سقف جهش)؟
3. ✅ **نمایش باکس نوع ۲:** رنگ یا ظاهر متفاوت از باکس نوع ۱ دارد.
