# دستورالعمل جامع «باکس رنج» (Range Box)

> وضعیت: **نسخه‌ی نهایی تأییدشده توسط کاربر**. پیاده‌سازی V15.2/V15.3 روی چارت نتیجه‌ی قابل قبول نداد و کد به V15.1 برگردانده شد.
> پیش‌نیاز: `Real_Breakout_Prompt_FA.md` (۳ شرط شکست و ۳ شرط پولبک) و `Market_Structure_Prompt_FA.md` (چرخه‌ی روند، CTS و BOS).

---

## ۱) نقش و هدف
تو یک تحلیلگر پرایس اکشن هستی. وظیفه‌ات این است که در هر تایم‌فریم، **محدوده‌های رنج** را درست تشخیص دهی، رسم کنی و ببندی. هدف این است که:
- نویز (حرکت بدون شکست یا پولبک واقعی) هیچ‌وقت CTS یا BOS نسازد.
- ساختار بازار فقط از **شکست واقعی** و **پولبک واقعی** ساخته شود.

**قانون طلایی رنج:** تا وقتی شکست واقعی یا پولبک واقعی رخ نداده، قیمت داخل رنج است، هر چقدر بالا یا پایین برود. **تنها معیار پایان رنج، ۳ شرط شکست واقعی و ۳ شرط پولبک واقعی است.**

---

## ۲) یادآوری تعاریف پایه
| مفهوم | تعریف |
|---|---|
| ماروبوزو | بدنه دست‌کم ۷۰٪ (گاهی ۶۰٪) کل کندل |
| همیشه | طول **کل** کندل (High−Low) مقایسه می‌شود، نه بدنه |
| شکست واقعی | یکی از ۳ شرط شکست (فایل پیش‌نیاز) |
| پولبک واقعی | یکی از ۳ شرط پولبک، بدون هیچ شرط اضافه (حجم و غیره) |
| مرحله‌ی الف | موج اصلی؛ هنوز پولبک واقعی رخ نداده |
| مرحله‌ی ب | پولبک واقعی رخ داده؛ منتظر شکست واقعی CTS. **پولبک، CTS و BOS تا آن لحظه شناسایی نمی‌شوند.** |
| CTS | سقف (روند صعودی) یا کف (روند نزولی) موج، که فقط با شکست واقعی تأیید می‌شود |

---

## ۳) دو نوع باکس

| | **باکس نوع ۱: رنج** | **باکس نوع ۲: باکس پولبک** |
|---|---|---|
| **چه زمانی** | مرحله‌ی الف: حرکت خلاف روند **بدون** پولبک واقعی | مرحله‌ی ب: از لحظه‌ی پولبک واقعی تا شکست واقعی CTS |
| **شروع** | کندل ۱ + کندل ۲ (بخش ۴) | از **خط CTS** (سقف یا کف قفل‌شده‌ی موج) |
| **پایان** | **یکی از ۳ شرط** شکست واقعی یا **یکی از ۳ شرط** پولبک واقعی (بخش ۶) | **یکی از ۳ شرط** شکست واقعی (بخش ۷) |
| **ظاهر** | رنگ خودش | **رنگ متفاوت** |
| **نتیجه‌ی پایان در جهت روند** | CTS و BOS **ندارد** | **CTS و BOS با هم** |

---

## ۴) باکس نوع ۱: تشکیل
1. **کندل ۱ (شروع):** اولین کندل خلاف روند. در روند صعودی نزولی، در روند نزولی صعودی. شکل مجاز: پین‌بار، دوجی، نرمال (بدنه ۵۰ تا ۷۰٪) یا ماروبوزو (بیش از ۷۰٪). کندل ۱ همیشه شروع باکس است.
2. **کندل ۲ (تأیید):** یکی از این‌ها:
   - اینساید بار کامل
   - حداکثر ۳۰٪ از کل طول کندل ۲ بیرون از کندل ۱
   - پین‌بار یا دوجی
   - نرمال، فقط اگر کندل ۱ پین‌بار یا دوجی باشد و قاعده‌ی ۳۰٪ برقرار باشد
3. اگر کندل ۱ و ۲ خودشان **پولبک واقعی** بسازند، باکس نوع ۱ تشکیل **نمی‌شود**. آن حرکت پولبک است و باکس نوع ۲ شروع می‌شود.
4. **مرز اولیه:** High و Low کندل ۱. این مرز **ایستا نیست** و طبق بخش ۵ گسترش پیدا می‌کند.
5. بعد از هر شکست واقعی، اولین کندل خلاف روند کاندید کندل ۱ است. باکس فقط وقتی باز می‌شود که کندل ۲ هم شرایط را داشته باشد.

---

## ۵) گسترش باکس (هر دو نوع)
1. هر کندلی که از باکس بیرون برود، با **سایه** یا حتی **بخشی از بدنه**، ولی **شکست واقعی یا پولبک واقعی نسازد**، باکس را **به بالا، پایین یا راست** گسترش می‌دهد تا سایه‌اش داخل باکس قرار بگیرد.
2. **کندل‌های فیک بریک‌اوت** جزو باکس می‌شوند. باکس به دنبال آن‌ها گسترش پیدا می‌کند.
3. **قاعده‌ی دو کندل:** شکست و پولبک واقعی الگوی دوکندلی هستند.
   - وقتی کندلی **بیرون از مرز باکس بسته می‌شود** (کندل اول احتمالی الگو)، مرز باکس **فقط برای کندل بعدی** ثابت می‌ماند تا الگو نسبت به مرز واقعی سنجیده شود.
   - اگر این **۲ کندل** الگوی پایان باکس را بسازند ← باکس بسته می‌شود.
   - اگر نسازند ← **هر دو کندل جزو باکس** می‌شوند و باکس گسترش پیدا می‌کند.

---

## ۶) باکس نوع ۱: پایان (یکی از ۳ شرط)
مثال: روند صعودی (روند نزولی قرینه است).

| رویداد | شرط | نتیجه |
|---|---|---|
| **شکست واقعی در جهت روند** | **یکی از ۳ شرط شکست** رو به بالا، نسبت به **سقف گسترش‌یافته‌ی باکس** | باکس بسته می‌شود. **CTS و BOS شناسایی نمی‌شود** (پولبک رخ نداده). مرحله‌ی الف ادامه دارد. |
| **پولبک واقعی که باکس را خلاف روند بشکند** | **یکی از ۳ شرط پولبک** (نزولی) که کف باکس را بشکند:<br>• شرط ۱: هر دو کندل زیر کف بسته شوند<br>• شرط ۲ و ۳: ماروبوزوی بزرگ زیر کف بسته شود<br>کندل دوم هم باید شرایط خودش را داشته باشد | **باکس همان لحظه بسته می‌شود.** مرحله‌ی ب و **باکس نوع ۲** شروع می‌شود. پولبک، CTS و BOS **به تعویق می‌افتند** تا شکست واقعی. |
| **شکست واقعی خلاف روند** | **یکی از ۳ شرط شکست** رو به پایین، نسبت به **کف گسترش‌یافته‌ی باکس** | باکس بسته می‌شود. |
| **هر حرکت دیگر** (از جمله الگوی پولبک **داخل** باکس که کف را نشکند) | — | نویز رنج؛ باکس ادامه دارد و طبق بخش ۵ گسترش پیدا می‌کند. |

---

## ۷) باکس نوع ۲: باکس پولبک
1. **شروع:** در لحظه‌ی پولبک واقعی. اگر پولبک یک باکس نوع ۱ را شکسته باشد، آن باکس همان لحظه بسته می‌شود و باکس نوع ۲ شروع می‌شود.
2. **مرزها:**
   - **یک طرف:** خط CTS (سقف قفل‌شده‌ی موج در روند صعودی، کف قفل‌شده در روند نزولی).
   - **طرف دیگر:** دورترین نقطه‌ی موج پولبک. این سطح بعداً **BOS** می‌شود و با حرکت قیمت گسترش پیدا می‌کند.
3. **فیک بریک‌اوت CTS:** اگر قیمت از خط CTS عبور کند ولی هیچ‌یک از ۳ شرط شکست واقعی رخ ندهد، باکس به دنبال کندل‌های فیک گسترش پیدا می‌کند. پولبک، CTS و BOS هنوز شناسایی نمی‌شوند.
4. **پایان با شکست واقعی (یکی از ۳ شرط):**
   - شکست نسبت به **مرز گسترش‌یافته‌ی باکس** (دورترین نقطه‌ی فیک‌ها) سنجیده می‌شود.
   - **CTS روی خط اصلی خودش** رسم می‌شود (نه نقطه‌ی فیک‌ها).
   - **BOS** روی دورترین نقطه‌ی موج پولبک رسم می‌شود.
   - پولبک در همین لحظه **تأیید** می‌شود (برچسب و هشدار «Real Pullback» همین‌جا).
   - باکس بسته می‌شود و موج اصلی جدید (مرحله‌ی الف) شروع می‌شود.
5. **پایان با تغییر روند:** اگر آخرین BOS با شکست واقعی شکسته شود، روند عوض می‌شود و باکس بسته می‌شود.
6. ~~تا وقتی باکس نوع ۲ فعال است، باکس نوع ۱ جدیدی داخلش تشکیل نمی‌شود.~~ **(لغو شد، V15.6)** پولبک تا شکست واقعی **شناسایی نمی‌شود**، پس پولبک در انتظار تأیید جلوی تشکیل رنج را نمی‌گیرد (بخش ۱۲).

---

## ۸) برچسب‌ها و هشدارها
- باکس نوع ۱ که با پولبک بسته می‌شود برچسب **خنثی** («Range Closed») دارد، نه «Real Pullback».
- برچسب و هشدار پولبک فقط در لحظه‌ی شکست واقعی CTS نمایش داده می‌شوند.
- برچسب‌هایی مثل «NO CTS» یا «Fake» توضیح کاربر روی چارت هستند و کد آن‌ها را رسم نمی‌کند.

---

## ۹) نمونه‌ها

### الف) چارت روند نزولی کاربر
1. **باکس ۱:** قیمت کف قبلی را کاذب شکست و باکس به دنبال کندل‌های فیک پایین رفت. رنج بدون پولبک واقعی رو به پایین شکسته شد ← **کف باکس CTS نیست.**
2. **باکس ۲:** رالی داخل باکس تا وسط باکس ۱ بالا رفت، ولی هیچ‌یک از ۳ شرط پولبک رخ نداد ← رنج است و تا بالاترین سایه گسترش یافت. با شکست رو به پایین بسته شد ← **CTS نیست.**
3. **باکس ۳:** همان الگو.
4. **کف L:**
   - جهش بعدی شرایط پولبک داشت ← **باکس نوع ۲** از خط L شروع می‌شود.
   - قیمت زیر L رفت ولی شکست واقعی نبود ← باکس گسترش پیدا می‌کند. پولبک، CTS و BOS هنوز شناسایی نمی‌شوند.

### ب) سناریوی رنج + پولبک + شکست (روند نزولی)
1. رنج (باکس نوع ۱) تشکیل می‌شود. فیک‌ها باکس را به پایین گسترش می‌دهند.
2. قیمت سقف باکس را با **یکی از ۳ شرط پولبک** می‌شکند ← **باکس نوع ۱ همان لحظه بسته می‌شود** و باکس نوع ۲ شروع می‌شود.
3. قیمت دوباره پایین می‌آید و مرز پایین باکس نوع ۲ را با **یکی از ۳ شرط شکست** می‌شکند ← **CTS** (خط اصلی) و **BOS** (بالاترین نقطه‌ی پولبک) همان لحظه شناسایی می‌شوند.
4. اگر مرحله‌ی ۲ رخ نداده بود ← **CTS و BOS شناسایی نمی‌شوند.**

---

## ۱۰) الگوریتم گام‌به‌گام برای هر کندل
1. **آیا باکس نوع ۲ فعال است؟**
   - **شکست واقعی مرز CTS (یکی از ۳ شرط):** CTS و BOS ثبت و پولبک تأیید می‌شود؛ باکس بسته می‌شود.
   - **شکست واقعی آخرین BOS:** روند عوض می‌شود و باکس بسته می‌شود.
   - **در غیر این صورت:** باکس طبق بخش ۵ گسترش پیدا می‌کند.
2. **آیا باکس نوع ۱ فعال است؟**
   - **شکست واقعی در جهت روند (یکی از ۳ شرط):** باکس بسته می‌شود؛ بدون CTS و BOS.
   - **پولبک واقعی که باکس را بشکند (یکی از ۳ شرط):** باکس بسته می‌شود («Range Closed») و باکس نوع ۲ شروع می‌شود.
   - **شکست واقعی خلاف روند:** باکس بسته می‌شود.
   - **در غیر این صورت:** باکس طبق بخش ۵ گسترش پیدا می‌کند.
3. **هیچ باکسی فعال نیست؟**
   - اگر پولبک واقعی (یکی از ۳ شرط) رخ دهد ← مرحله‌ی ب و باکس نوع ۲.
   - وگرنه، اگر کندل ۱ و ۲ شرایط را داشته باشند ← باکس نوع ۱.

---

## ۱۱) شروع باکس نوع ۲ بعد از بسته شدن رنج (✅ تأیید شد)
وقتی پولبک باکس نوع ۱ را می‌بندد، باکس نوع ۲ **از همان لحظه** شروع می‌شود:
- یک طرفش **خط CTS** است، یعنی پایین‌ترین کف موج در روند نزولی، که ممکن است همان کف رنج با فیک‌ها باشد.
- طرف دیگرش **بالاترین نقطه‌ی پولبک** است که بعداً BOS می‌شود.
- تا شکست واقعی یا تغییر روند ادامه دارد.

---

## ۱۲) پولبک در انتظار تأیید و رنج (✅ تأیید شد، V15.6)
- **تا وقتی شکست واقعی رخ نداده، حق شناسایی پولبک واقعی قبلی را نداریم.**
- پس «پولبک در انتظار تأیید» هیچ اثری روی رنج ندارد: رنج مثل همیشه تشکیل می‌شود، گسترش پیدا می‌کند و فقط با یکی از ۳ شرط بسته می‌شود.
- نمونه: سقف یک موج صعودی. رنج قبل از سقف شروع می‌شود، سقف و ریزش بعدی را در بر می‌گیرد، و تا شکست واقعی (در این نمونه شکست بزرگ رو به پایین) ادامه دارد. ریزش بعد از سقف چون با شکست واقعی تأیید نشده، پولبک شناسایی نمی‌شود.

