# دستورالعمل «باکس رنج» (Range Box) — پیش‌نویس در حال تکمیل

> وضعیت: قواعد ✅ را کاربر تأیید کرده و در نسخه‌ی V15.2 پیاده شده‌اند. موارد ❓ منتظر جواب هستند.
> پیش‌نیاز: `Real_Breakout_Prompt_FA.md` (۳ شرط شکست و ۳ شرط پولبک) و `Market_Structure_Prompt_FA.md`.

---

## ۱) تعریف
رنج یعنی حرکت خلاف روند **بدون پولبک واقعی**. تا وقتی **شکست واقعی** یا **پولبک واقعی** (هر کدام با ۳ شرط اصلی) رخ ندهد، رنج ادامه دارد.

## ۲) تشکیل رنج
- **کندل ۱ (شروع رنج):** اولین کندل خلاف روند. در روند صعودی کندل نزولی، در روند نزولی کندل صعودی. شکل مجاز: پین‌بار، دوجی، نرمال (بدنه ۵۰ تا ۷۰٪) یا ماروبوزو (بیش از ۷۰٪).
- **کندل ۲ (تأیید بلاتکلیفی):** یکی از این‌ها:
  - اینساید بار کامل
  - حداکثر ۳۰٪ از کل طول کندل ۲ بیرون از کندل ۱ باشد
  - پین‌بار یا دوجی
  - نرمال، فقط اگر کندل ۱ پین‌بار یا دوجی باشد و قاعده‌ی ۳۰٪ برقرار باشد
- **سقف و کف اولیه:** High و Low **کندل ۱**.
- اگر کندل ۱ یا ۲ پولبک واقعی بسازد، رنج تشکیل **نمی‌شود**. آن حرکت پولبک است.
- بعد از یک شکست واقعی، اولین کندل خلاف روند کاندید کندل ۱ است. رنج فقط وقتی باز می‌شود که کندل ۲ هم شرایط را داشته باشد.

## ۳) گسترش رنج ✅
- **معیار اصلی پایان رنج فقط ۳ شرط شکست واقعی یا پولبک واقعی است.**
- هر کندلی که از رنج بیرون برود (با سایه، یا حتی بخشی از بدنه) ولی شکست واقعی یا پولبک واقعی نسازد، رنج را **به بالا، پایین یا راست گسترش می‌دهد**.
- **قاعده‌ی دو کندل:**
  - شکست و پولبک واقعی الگوهای دوکندلی هستند. وقتی کندلی بیرون از مرز رنج بسته می‌شود (کندل اول احتمالی الگو)، مرز رنج **فقط برای کندل بعدی** ثابت می‌ماند تا الگو نسبت به مرز واقعی سنجیده شود.
  - اگر این **۲ کندل** شکست واقعی یا پولبک واقعی بسازند ← رنج بسته می‌شود.
  - اگر نسازند ← هر دو کندل جزو رنج می‌شوند و رنج گسترش پیدا می‌کند.

## ۴) پایان رنج ✅
| رویداد | نتیجه |
|---|---|
| **شکست واقعی در جهت روند** (بدون پولبک واقعی قبلی) | رنج بسته می‌شود. **CTS و BOS شناسایی نمی‌شود.** |
| **پولبک واقعی که رنج را خلاف روند بشکند** | رنج بسته می‌شود. **پولبک واقعی ساختاری** است (بخش ۱۱ پرامپت ساختار). |
| **شکست واقعی خلاف روند** | رنج بسته می‌شود. |
| الگوی پولبک **داخل** رنج که رنج را نشکند | نویز رنج؛ رنج ادامه دارد. |

### شکستن رنج با الگوی پولبک
- **شرط ۱ (دو ماروبوزوی پیوسته و مشابه):**
  - هر دو کندل بیرون از رنج بسته شوند.
  - کندل دوم شرایط خودش را داشته باشد: در پولبک نزولی، کندل دوم پایین‌تر از Close کندل اول بسته شود و طول کل کندل دوم دست‌کم ۷۰٪ طول کل کندل اول باشد.
  - همیشه طول کل کندل مقایسه می‌شود، نه بدنه.
- **شرط ۳ (ماروبوزوی بزرگ + کندل کوچک معکوس):**
  - ماروبوزوی بزرگ بیرون از رنج بسته شود.
  - کندل دوم کوچک (۳۰٪ یا کمتر از طول کل ماروبوزو) باشد و سر دیگرش از ۵۰٪ ماروبوزو عبور نکند.
- **شرط ۲ (ماروبوزوی بزرگ + کندل کوچک مشابه):** ماروبوزوی بزرگ بیرون از رنج بسته شود. ❓ جزئیات کندل دوم در بخش ۶.

## ۵) نمایش (فعلی)
- باکس خاکستری در حالت فعال.
- بعد از بسته شدن:
  - سبز (شکست صعودی)
  - قرمز (شکست نزولی)
  - نارنجی (پولبک)
- عدد «۱» روی کندل ۱ و «R» پایین چارت.

## ۶) سؤال باز ❓
**شرط ۲ هنگام شکستن رنج:** در متن شما برای کندل دوم شرط ۲ (کندل کوچک مشابه) آمده است: «کندل اول پایین‌تر از کندل دوم بسته شود و کندل دوم حداقل ۷۰٪ کندل اول باشد». این شرط‌های **شرط ۱** هستند. کندل «کوچک» با طول «حداقل ۷۰٪» هم با هم تناقض دارند. کدام درست است؟
- **الف)** شرط ۲ هنگام شکستن رنج همان شرط ۲ عادی است: ماروبوزوی بزرگ بیرون از رنج بسته شود، و سر دیگر کندل دوم (کوچک و هم‌جهت) از ۵۰٪ ماروبوزو عبور نکند. این همان چیزی است که الان در کد است.
- **ب)** چیز دیگری مد نظر شماست.
