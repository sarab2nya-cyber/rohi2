# دستورالعمل «شناخت ساختار بازار» (CTS / BOS) — نسخه‌ی تأییدشده

> وضعیت: **تأییدشده توسط کاربر**. هنوز در کد پیاده نشده است.
> پیش‌نیاز: تعریف «شکست واقعی» و «پولبک واقعی» (۳ شرط) در فایل `Real_Breakout_Prompt_FA.md`.

---

## ۱) اصطلاحات
| اصطلاح | معنا در روند صعودی | معنا در روند نزولی |
|---|---|---|
| **موج اصلی (Impulse)** | حرکت رو به بالا | حرکت رو به پایین |
| **موج پولبک** | حرکت اصلاحی رو به پایین | حرکت اصلاحی رو به بالا |
| **سقف موج (H\*)** | بالاترین High موج اصلی جاری | — |
| **کف موج (L\*)** | — | پایین‌ترین Low موج اصلی جاری |
| **CTS (Recent High / Recent Low)** | سقف موجی که بعد از پولبک واقعی **با شکست واقعی شکسته شد** | کف موجی که بعد از پولبک واقعی با شکست واقعی شکسته شد |
| **BOS (Key Level)** | پایین‌ترین کف کل موج پولبک، که در لحظه‌ی شکست CTS ثبت می‌شود | بالاترین سقف کل موج پولبک، که در لحظه‌ی شکست CTS ثبت می‌شود |
| **شکست کاذب (Fake Breakout)** | عبور از سطح (با سایه یا بدنه) **بدون** هیچ‌یک از ۳ شرط شکست واقعی | همان |

**قانون طلایی:** CTS و BOS **فقط با هم** و **فقط در لحظه‌ی شکست واقعی CTS** شناسایی می‌شوند. تا آن لحظه هیچ‌کدام برچسب نمی‌خورند.

---

## ۲) چرخه‌ی روند صعودی (مراحل)

### مرحله‌ی الف: موج اصلی (در انتظار پولبک)
1. سقف موج (H\*) را دنبال کن. هر High بالاتر، H\* را به‌روز می‌کند.
2. اگر در حرکت خلاف روند **یکی از ۳ شرط پولبک واقعی** رخ دهد، به **مرحله‌ی ب** برو.
   - این شرط ممکن است ابتدای ریزش رخ دهد یا وسط آن، ولی **باید** داخل همان ریزش باشد.
   - از این لحظه H\* **قفل می‌شود** و «CTS در انتظار شکست» است.
3. اگر حرکت خلاف روند **بدون** هیچ‌یک از ۳ شرط پولبک باشد (کندل‌های معمولی، اصلاح آرام)، آن حرکت **رنج** است، نه پولبک:
   - اگر رنج با **شکست واقعی در جهت روند** (رو به بالا) تمام شود و پولبک واقعی رخ نداده باشد، **هیچ CTS و BOS‌ای شناسایی نمی‌شود**. قیمت در مرحله‌ی الف ادامه می‌دهد و H\* با سقف‌های جدید به‌روز می‌شود.
   - اگر داخل رنج یا هنگام خروج از آن **یکی از ۳ شرط پولبک** رخ دهد، به مرحله‌ی ب برو. H\* = بالاترین نقطه‌ی قبل از پولبک، شامل سقف رنج.

### مرحله‌ی ب: موج پولبک (در انتظار شکست CTS)
1. **پایین‌ترین کف کل موج پولبک (L\*)** را از سقف H\* تا لحظه‌ی شکست واقعی H\* دنبال کن. رنج‌های داخل موج هم شامل می‌شوند.
   - هر چقدر موج طول بکشد (حتی صدها کندل) و هر رنجی داخلش تشکیل شود، فقط L\* مهم است.
   - **موج‌های داخلی** اصلاح در این تایم‌فریم شناسایی **نمی‌شوند**. ساختار آن‌ها در تایم‌فریم کوچک‌تر بررسی می‌شود.
   - اگر پولبک واقعی رخ داده ولی شکست واقعی H\* هنوز رخ نداده، شناسایی BOS **به تعویق می‌افتد**.
2. **عبور کاذب از H\*** (سایه یا بسته‌شدن بالای آن بدون شرط شکست واقعی):
   - H\* **تغییر نمی‌کند** و CTS قبلی معتبر می‌ماند.
   - **هیچ BOS‌ای شناسایی نمی‌شود.** شاید این آخرین موج صعودی باشد و بعد کف قبلی شکسته شود.
3. **شکست واقعی H\*** (یکی از ۳ شرط شکست واقعی، با خط BOI = H\*):
   - **CTS** در H\* تأیید و رسم می‌شود (Recent High).
   - **BOS** در L\* تأیید و رسم می‌شود (Key Level).
   - موج اصلی جدید شروع می‌شود و به **مرحله‌ی الف** برمی‌گردیم.
4. **عبور کاذب از BOS قبلی** (پایین‌تر رفتن از آخرین Key Level بدون شرط شکست واقعی):
   - روند **عوض نمی‌شود**. L\* می‌تواند پایین‌تر از BOS قبلی باشد و در شکست بعدی CTS همین کف جدید BOS می‌شود. نمونه: BOS3 کمی پایین‌تر از BOS2 در چارت شما.
5. **شکست واقعی آخرین BOS رو به پایین** (یکی از ۳ شرط شکست واقعی، با خط BOI = آخرین Key Level) ← **تغییر روند به نزولی** (بخش ۳).

---

## ۳) تغییر روند (صعودی ← نزولی)
- وقتی آخرین BOS صعودی با **شکست واقعی** شکسته شود، روند نزولی می‌شود.
- **تغییر روند قبل از پولبک هم معتبر است:** حتی اگر هنوز پولبک واقعی رخ نداده (مرحله‌ی الف)، شکست واقعی آخرین BOS روند را عوض می‌کند.
- **سقف موجی که BOS را شکست** (H\*، بالاترین نقطه‌ی آخرین موج صعودی) **اولین BOS نزولی** است. شکست واقعی آن رو به بالا روند را دوباره صعودی می‌کند.
- این سقف در لحظه‌ی تغییر روند **برچسب نمی‌خورد**. برچسب‌گذاری فقط وقتی انجام می‌شود که کف ایجادشده **دوباره با شکست واقعی شکسته شود و کف پایین‌تری بسازد** (جزئیات در بخش ۷).
- **کف همان حرکت نزولی** اولین کف موج نزولی (L\*) است. بعد از یک پولبک صعودی واقعی و سپس شکست واقعی این کف، به **CTS نزولی (Recent Low)** تبدیل می‌شود.
- از اینجا چرخه‌ی بخش ۲ به‌صورت **قرینه** اجرا می‌شود:
  - سقف ↔ کف
  - بالا ↔ پایین
  - پولبک نزولی ↔ پولبک صعودی

---

## ۴) رنج در ساختار
- رنج یعنی حرکت خلاف روند **بدون** پولبک واقعی.
- هیچ CTS یا BOS‌ای **داخل** رنج شناسایی نمی‌شود.
- **خروج از رنج در جهت روند بدون پولبک واقعی** ← آن ناحیه هرگز CTS یا BOS نمی‌گیرد.
- **خروج از رنج خلاف روند با یکی از ۳ شرط پولبک** ← این همان پولبک واقعی است و مرحله‌ی ب شروع می‌شود.
- **رنجی که داخل موج پولبک (مرحله‌ی ب) تشکیل شود**، روی ساختار اثری ندارد. فقط کفش در محاسبه‌ی L\* می‌آید.

---

## ۵) زمان رسم
| عنصر | زمان رسم |
|---|---|
| CTS | فقط در لحظه‌ی **شکست واقعی** آن و ساختن سقف یا کف جدید |
| BOS | **دقیقاً همان لحظه** (کف یا سقف موج پولبک در آن لحظه نهایی است) |
| زیگزاگ | اتصال H\* و L\* تأییدشده |

---

## ۶) شروع چارت (✅ تأیید شد، بدون شمارش کندل)
در ابتدای چارت روند **نامشخص** است و با **خود قواعد** تعیین می‌شود، نه با تعداد کندل:
1. از اولین کندل، بالاترین سقف (H\*) و پایین‌ترین کف (L\*) دنبال می‌شود.
2. هر دو جهت هم‌زمان زیر نظر است:
   - اگر **پولبک نزولی واقعی** رخ دهد و بعد H\* با **شکست واقعی** شکسته شود ← **روند صعودی**. CTS در H\* و BOS در پایین‌ترین کف آن پولبک رسم می‌شود.
   - اگر **پولبک صعودی واقعی** رخ دهد و بعد L\* با **شکست واقعی** شکسته شود ← **روند نزولی** (قرینه).
3. **اولین چرخه‌ی کامل** (موج ← پولبک واقعی ← شکست واقعی) در هر جهت، روند را تعیین می‌کند. تا آن لحظه هیچ CTS یا BOS‌ای رسم نمی‌شود.

## ۷) برچسب‌گذاری بعد از تغییر روند (✅ مطابق چارت شما)
مثال روند صعودی ← نزولی:
- **A** = بالاترین سقف آخرین موج صعودی
- **L1** = کفی که بعد از شکست BOS صعودی ساخته شد
- **B** = سقف پولبک صعودی بعد از L1
- **L2** = کف پایین‌تر بعد از شکست واقعی L1

قواعد:
1. BOS صعودی (کف آخرین پولبک) با **شکست واقعی** شکسته می‌شود ← روند نزولی. خط این BOS تا کندل شکست کشیده می‌شود.
2. **A برچسب نمی‌گیرد.** فقط سطح برگشت روند است.
3. بعد از L1، پولبک صعودی واقعی تا B رخ می‌دهد.
4. L1 با **شکست واقعی** شکسته می‌شود و L2 ساخته می‌شود ← **CTS در L1** و **BOS در B** رسم می‌شود.
5. رنجی که داخل موج نزولی تشکیل شود و بدون پولبک واقعی رو به پایین شکسته شود، برچسب نمی‌گیرد.
6. عبور کاذب قیمت از خط BOS (مثلاً با سایه، بدون شکست واقعی) روند را برنمی‌گرداند.

## ۸) سطح برگشت روند قبل از اولین BOS جدید (✅ تأیید شد)
- بعد از تغییر روند و تا رسم اولین BOS جدید (B)، سطحی که شکست واقعی‌اش روند را دوباره برمی‌گرداند **A** است.
- بعد از رسم B، آن سطح **B** است.

---

## ۹) چرخه‌ی روند نزولی (قرینه‌ی کامل بخش ۲)

### مرحله‌ی الف: موج اصلی نزولی (در انتظار پولبک)
1. **کف موج (L\*)** را دنبال کن. هر Low پایین‌تر، L\* را به‌روز می‌کند.
2. اگر در حرکت رو به بالا **یکی از ۳ شرط پولبک واقعی صعودی** رخ دهد، به مرحله‌ی ب برو و L\* **قفل می‌شود**.
3. حرکت رو به بالا **بدون** شرط پولبک، **رنج** است. اگر رنج رو به پایین با شکست واقعی شکسته شود و پولبک واقعی رخ نداده باشد، هیچ CTS و BOS‌ای شناسایی نمی‌شود و L\* با کف‌های جدید به‌روز می‌شود.

### مرحله‌ی ب: موج پولبک صعودی (در انتظار شکست CTS)
1. **بالاترین سقف کل موج پولبک (H\*)** را از کف L\* تا لحظه‌ی شکست واقعی L\* دنبال کن. رنج‌های داخل موج هم شامل می‌شوند.
2. **عبور کاذب از L\*** (سایه یا بسته‌شدن زیر آن بدون شرط شکست واقعی): L\* تغییر نمی‌کند و BOS شناسایی نمی‌شود.
3. **شکست واقعی L\* رو به پایین**:
   - **CTS (Recent Low)** در L\*
   - **BOS (Key Level)** در H\* (سقف موج پولبک)
   - سپس برگشت به مرحله‌ی الف.
4. **عبور کاذب از BOS قبلی رو به بالا**: روند عوض نمی‌شود. H\* می‌تواند بالاتر از BOS قبلی باشد و در شکست بعدی CTS همین سقف BOS می‌شود.
5. **شکست واقعی آخرین BOS رو به بالا**، حتی قبل از پولبک ← **تغییر روند به صعودی**:
   - پایین‌ترین کف آخرین موج نزولی برچسب نمی‌گیرد و سطح برگشت روند است.
   - برچسب‌ها وقتی رسم می‌شوند که سقف جدید دوباره با شکست واقعی شکسته شود: CTS در آن سقف، BOS در کف پولبک بین آن‌ها.

---

## ۱۰) تایم‌فریم‌ها
- **هر تایم‌فریم باید تمام این قواعد را مستقل رعایت کند.** ساختار هر تایم‌فریم فقط از کندل‌های همان تایم‌فریم ساخته می‌شود.
- موج‌های داخلی یک اصلاح در تایم‌فریم بالاتر، در **تایم‌فریم پایین‌تر** با همین قواعد، ساختار کامل خودشان را دارند (CTS، BOS، پولبک، رنج).
- یک حرکت ممکن است در تایم‌فریم پایین‌تر چند چرخه‌ی کامل باشد و در تایم‌فریم بالاتر فقط یک موج پولبک.

